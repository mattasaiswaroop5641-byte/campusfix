{
  "name": "campusfix-backend",
  "version": "1.0.0",
  "description": "MongoDB & Express Backend API for CampusFix",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "cd .. && npm install && npm run build && cd server"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.9.5",
    "nodemailer": "^6.9.16"
  }
}